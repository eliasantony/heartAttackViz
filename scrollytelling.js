import { Controller, Scene } from 'scrollmagic';
import 'scrollmagic/scrollmagic/uncompressed/ScrollMagic.js';

// document.addEventListener('DOMContentLoaded', function () {
//     // Initialize the controller
//     const controller = new Controller();

//     // Get all sections
//     const sections = document.querySelectorAll('section');

//     // Create a scene for each section
//     sections.forEach((section) => {
//         new Scene({
//             triggerElement: section, // set the element that triggers
//             duration: '100%', // the scroll distance
//             triggerHook: 0, // start scene when the top of the trigger hits the top of the viewport
//         })
//         .setPin(section) // pins the element for the the scene's duration
//         .addTo(controller); // assign the scene to the controller
//     });
// });
